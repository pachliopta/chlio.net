<div class="status-card">
    <div>
        <span>{{ metadata.author.name }}</span>
        <span class="subscript color-secondary">@chlio.net</span>
    </div>
    <p>{{ statusContent | safe }}</p>
    <a href={{ statusUrl | urlize }}><span class="color-secondary subscript">{{ statusDate | postDate }}</span></a>
</div>
