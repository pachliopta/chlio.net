---
title: chlio's website
description: "chlio's place to talk about game development and butterflies"
eleventyImport:
    collections: ["_status", "_post"]
---

{% extends "default.njk" %}
{% block content %}
    <div class="container">
        {% include "separator.njk" %}
        <p>Welcome to chlio.net</p>
        <div class="section">
            <h3>latest posts</h3>
            {% set postlist = collections._post | head(-3) %}
            {%- for post in postlist | reverse -%}
                <a href={{ post.url | urlize }}>
                    <span class=""> <b>{{ post.data.title }}</b> - <span class="color-secondary subscript"> {{ post.data.date | htmlDateString }} </span> </span>
                </a>
            {%- endfor -%}
            <a href="/blog">
                <p>
                    more posts <span aria-hidden="true">>></span>
                </p>
            </a>
        </div>
        <div class="section">
            <h3>latest status</h3>
            {% set postlist = collections._status | head(-1) %}
            {%- for item in postlist | reverse -%}
              {% set c = item.data | concat({content: item.content, url: item.url}) %}
              {% renderTemplate "njk", c %}
                {% include "status-card.njk" %}
              {% endrenderTemplate  %}
            {%- endfor -%}
            <a class="padding-top" href="/status">
                <span> more statuses <span aria-hidden="true">>></span></span>
            </a>
        </div>
    </div>
{% endblock content %}
